@model List<VariableViewModel>

@if (Model.Count > 0)
{
    <div class="alert alert-info">
        <strong>Available variables</strong>

        <ul>
            @foreach (var item in Model)
            {
                <li>
                    {@item.Name} - @item.Value
                </li>
            }
        </ul>
    </div>
}